# 2026H2W9 打卡｜得到课程同步到得到大脑（演练记录）

提交人（群昵称或真实姓名，二选一）：**杨明**
GitHub 账号：**39291893@qq.com**
提交日期：2026-09-10
说明：本次为**演练/模拟打卡**，记录真实执行过程；其中预检与同步因平台会员门槛未完成，已如实标注，未伪造任何结果。

---

## 1. 我处理的是自己有权访问的哪类课程（不必公开课程链接）

得到 App（dedao.cn）上**我自己已购买**的一门**年度日更投资类课程**，形态是"发刊词 + 课前加餐 + 每日正文 + 定期问答/直播回放"的混合目录。

- 全程复用浏览器里**已登录的本人会话**，只发现自己有权访问的已发布正文
- 不处理未购内容，不绕过付费墙、登录或设备验证
- 本次只提取**目录级安全字段**，不保存课程正文

## 2. 我的工作流：浏览器 → 清单 → 预检 → 得到大脑 → 核验

| 阶段 | 做法 |
|---|---|
| **浏览器** | 用调试端口连接已登录的浏览器会话 → 从任一课程文章回到课程目录 → 只提取安全字段（`order_num`、`title`、`enid`、发布状态、发布时间），在浏览器侧裁剪后再返回，不把完整响应带出 |
| **清单** | 生成规范网址 `https://www.dedao.cn/course/article?id=<enid>`，按 `enid` 去重，排除未发布/推荐/评论/其他课程 → 存为 `course-articles.json` |
| **预检** | `sync_getnote.py --dry-run`：校验网址确属课程正文、合并重复、用标题搜索 + 来源网址核对判重。**只读取，不写入任何笔记** |
| **得到大脑** | 预检确认后才批量提交网址，由 Get笔记自行提取全文；并发上限 3，失败重试一次，写入断点状态文件支持续传 |
| **核验** | 不把"退出码 0"当成功；以脚本最终状态为准，核对任务状态、笔记 ID、来源网址与已生成内容，必要时按标题反查落库结果 |

抽查首篇 / 中段 / 末篇三处，标题与清单完全一致，确认登录态有效且能打开正确正文。

## 3. 课程规划 / 已发布 / 未发布数量

| 指标 | 数量 |
|---|---|
| 课程规划总讲数 | **300** |
| 当前已发布 | **224** |
| 尚未发布 | **76** |

规划数与已发布数的差额表示课程仍在更新，**不把差额当成抓取失败**；未发布部分只报告，不伪造网址。

## 4. 预检结果：已存在 / 待新增

| 项目 | 结果 |
|---|---|
| 已存在 | **未知** |
| 待新增 | **未知** |

**原因**：预检调用 Get笔记 OpenAPI 时返回 `10201 not_member —— OpenAPI 仅对会员开放`，查重在第一步即中断，无法得出判定。清单本身已就绪（224 条、enid 全唯一），会员开通后无需重抓，直接重跑即可续上。

## 5. 同步结果：新增 / 重复 / 失败

| 项目 | 结果 |
|---|---|
| 新增 | **0**（未执行）|
| 重复 | **未知**（查重未完成）|
| 失败 | **无**（未发起任何提交）|

同步状态文件 `.sync-state.json` **未生成**，可确认零写入——没有产生任何笔记，也没有向得到大脑提交过一个网址。

## 6. 我没有公开或保存到 GitHub 的敏感、版权或账号内容

以下内容**全程未导出、未落盘、未进入对话**：

- `Cookie`、`Authorization` 请求头
- `dd_article_token`、请求签名参数、设备标识
- 完整网络请求/响应正文
- 课程文章全文
- API key、账号凭据、可识别个人信息

写入本地清单的**只有**：`order`、`title`、`enid`、规范网址。打卡报告不含课程链接清单，也不含任何凭据。

## 7. 我遇到的真实阻塞及处理方式

| 阻塞 | 处理 |
|---|---|
| 浏览器调试端口连不上，WebSocket 握手 403 | 启动时补 `--remote-allow-origins=*` |
| 加了调试参数仍不生效 | 浏览器单实例会吞掉新参数 → 先彻底结束全部进程，再带参数启动 |
| 浏览器自动化工具连接频繁超时 | 改为用 Python 直连 CDP 执行与取数，稳定可控 |
| 目录分页取不全（一次最多 50 条，取多了返回空） | 摸清游标语义：`max_order_num` 是**下界**，`since_order_num` 是**上界**；构造干净请求体按游标翻页，取全 224 条 |
| 脚本在 Windows 上调用 `getnote` 报"找不到文件" | CLI 是 `.cmd`，无法直接被创建进程执行 → 修复为 Windows 走 shell 并做参数转义（其他平台逻辑不变）|
| **Get笔记 OpenAPI 仅对会员开放（未解决）** | 停在执行前，不伪造结果；等待开通后一条命令续跑 |

## 8. 我理解的"Skill 串联"是

把**一个完整任务拆成各司其职的几段**，用"流程技能"串起"原子技能"和外部能力：

- **流程编排**（本次的同步 Skill）：负责顺序、去重、断点续传、失败重试与最终汇总，是"总指挥"
- **原子能力**（认证 / 保存 / 搜索 / 知识库 / 标签）：每个只做一件事，可被任意流程单独调用
- **外部能力**（浏览器控制）：只负责**发现**网址，不碰内容

串联的关键是**边界清晰**：浏览器只发现不取正文，流程只判定不复制内容，原子技能只写入不删改既有数据；每一段都能单独失败、单独重跑，状态文件保证中断后不重复劳动。

## 9. 下次可复用的一条规则

> **先把"禁止项"和验收标准写进流程，再动手；批量执行前，先用一次最小调用验证权限与链路是否真的通。**

本次就是靠"最小调用验证"提前发现了会员门槛，避免了对 224 条网址做无意义的提交尝试；而"不把退出码当成功""疑似重复宁可跳过""不导出 Cookie/签名"这些边界，都是一开始就写死在流程里的。

---

## 脱敏说明

本报告不含课程链接清单、课程全文、Cookie、令牌、签名、API key 或任何可识别个人信息；仅记录流程、数量与问题处理方式。
